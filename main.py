"""Legacy stub. Run the API from services/api (see README.md)."""

if __name__ == "__main__":
    print("skiMate: `./scripts/run_api.sh` or `cd services/api && uvicorn skiplanner_api.main:app --reload --port 8040`")
